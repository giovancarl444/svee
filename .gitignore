# Secrets — NEVER commit real credentials (Constraint §3)
.env
.env.local
.env.*.local
*.env.local
secrets/
.mcp-auth/

# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
*.tsbuildinfo
.next/
out/
next-env.d.ts

# Runtime / data — the message store never lives in the repo
data/
pgdata/
*.sqlite
*.sqlite-*

# OAuth / session artifacts (WhatsApp linked-device, Gmail tokens on disk)
.auth/
wa-session/

# Test / coverage
coverage/
.vitest/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.log
