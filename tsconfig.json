{
  "//": "Root typecheck pass for the non-Next packages + workers. The dashboard typechecks itself via Next (see apps/dashboard/tsconfig.json).",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["packages/*/src/**/*.ts", "apps/workers/src/**/*.ts"]
}
